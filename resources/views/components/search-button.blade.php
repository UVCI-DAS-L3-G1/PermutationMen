<div>
    @if ({{ }})
    <x-jet-button disabled {{}} class="opacity-50 cursor-not-allowed">
        {{__('Afficher')}}
    </x-jet-button>
    @else
    <x-jet-button>
        {{__('Afficher')}}
    </x-jet-button>
    @endif
</div>
